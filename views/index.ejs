<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Notes</title>
    <link rel="stylesheet" href="styles/styles.css">
</head>
<body>
    <h1>Liblary</h1>
    <p>Notes your finished books 📚.</p>
    <% result.forEach((data) => { %>
        <div class="book">
            <% const callBookCover = () => {
                return `https://covers.openlibrary.org/b/isbn/${data.isbn}-M.jpg`;
            }
            let img = callBookCover();
            %>
            <h2><%= data.title %></h2>
            <img src=<%= img %> alt="book cover">
            <h3><%= data.author %></h3>
            <h3><%= data.publisher %></h3>
            <p><%= data.review %></p>
            <p><%= data.rating %></p>
        </div>
    <% }) %>
    
</body>
</html>